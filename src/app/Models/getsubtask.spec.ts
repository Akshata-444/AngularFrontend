/*import { Getsubtask } from './getsubtask';

describe('Getsubtask', () => {
  it('should create an instance', () => {
    // Provide values for title, description, status, and taskId
    const title = 'Sample Title';
    const description = 'Sample Description';
    const taskId = 123; // Provide the appropriate taskId value

    expect(new Getsubtask(title, description, taskId)).toBeTruthy();
  });
});
*/
